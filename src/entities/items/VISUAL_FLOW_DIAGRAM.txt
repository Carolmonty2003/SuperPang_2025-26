# Items System - Visual Flow Diagram

```
╔════════════════════════════════════════════════════════════════════════════╗
║                      ITEMS & DROPS SYSTEM ARCHITECTURE                      ║
╚════════════════════════════════════════════════════════════════════════════╝


┌─────────────────────────────────────────────────────────────────────────────┐
│                           1. ENEMY DESTRUCTION                              │
└─────────────────────────────────────────────────────────────────────────────┘

    Enemy.destroy()
         │
         ├──> scene.dropper.dropFrom(this, x, y)
         │
         └──> Random roll (40% base chance)
                   │
                   ├─ PASS → Continue
                   │
                   └─ FAIL → No drop, return


┌─────────────────────────────────────────────────────────────────────────────┐
│                         2. LOOT TABLE SELECTION                             │
└─────────────────────────────────────────────────────────────────────────────┘

    Weighted Random Selection:
    
    ┌─────────────────────────────────────────────────────────┐
    │  Item Type           │  Weight  │  Probability          │
    ├──────────────────────┼──────────┼───────────────────────┤
    │  Score Small (100)   │    40    │   ~39%   ████████    │
    │  Score Medium (250)  │    25    │   ~24%   █████       │
    │  Score Large (500)   │    15    │   ~15%   ███         │
    │  Score Special (1000)│     5    │    ~5%   █           │
    │  Speed Boost         │     8    │    ~8%   ██          │
    │  Weapon Upgrade      │     5    │    ~5%   █           │
    │  Shield              │     3    │    ~3%   █           │
    │  Extra Life          │     2    │    ~2%   █           │
    └──────────────────────┴──────────┴───────────────────────┘
                                      Total Weight: 103


┌─────────────────────────────────────────────────────────────────────────────┐
│                           3. ITEM CREATION                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    Dropper.createItem(itemType, x, y, variant)
         │
         ├──> new ScoreBonus(scene, x, y, variant)
         ├──> new PowerUpLife(scene, x, y)
         ├──> new PowerUpShield(scene, x, y)
         ├──> new PowerUpSpeed(scene, x, y)
         └──> new PowerUpWeapon(scene, x, y)
                   │
                   ├──> BaseItem Constructor
                   │     ├─ Add to scene
                   │     ├─ Enable physics body
                   │     ├─ Set gravity, bounce, velocity
                   │     ├─ Configure TTL timer
                   │     └─ Create visual effects (pulse, rotate)
                   │
                   └──> Track in dropper.activeItems[]


┌─────────────────────────────────────────────────────────────────────────────┐
│                          4. ITEM LIFECYCLE                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    SPAWN
      │
      ├──> Apply initial velocity (upward + random horizontal)
      │
      ├──> Fall with gravity & bounce physics
      │
      ├──> Visual: Pulse/rotate animation
      │
      ├──> TTL countdown (8-10 seconds)
      │         │
      │         ├─ Last 2 seconds: Start blinking
      │         │
      │         └─ TTL = 0: Auto-despawn (fade out)
      │
      └──> WAIT FOR PICKUP or DESPAWN


┌─────────────────────────────────────────────────────────────────────────────┐
│                           5. PICKUP DETECTION                               │
└─────────────────────────────────────────────────────────────────────────────┘

    Scene.update()
      │
      └──> dropper.update(hero)
             │
             └──> For each activeItem:
                    │
                    └──> item.checkPickup(hero)
                           │
                           └──> Phaser.Geom.Intersects.RectangleToRectangle(item, hero)
                                  │
                                  ├─ NO COLLISION → Continue
                                  │
                                  └─ COLLISION → item.pickup(hero)


┌─────────────────────────────────────────────────────────────────────────────┐
│                           6. PICKUP FLOW                                    │
└─────────────────────────────────────────────────────────────────────────────┘

    item.pickup(hero)
      │
      ├──> Set consumed = true, active = false
      │
      ├──> Call item.onPickup(hero)  [POLYMORPHIC - each item implements this]
      │      │
      │      ├─ ScoreBonus    → hero.addScore(points)
      │      │                  + Show floating "+XXX" text
      │      │
      │      ├─ PowerUpLife   → hero.addLife(1)
      │      │                  + Show "+1 LIFE" text
      │      │
      │      ├─ PowerUpShield → hero.setShield(5000ms)
      │      │                  + Cyan tint + pulsing
      │      │                  + Timer to auto-revert
      │      │
      │      ├─ PowerUpSpeed  → hero.applySpeedBuff(1.5x, 8000ms)
      │      │                  + Store original speed
      │      │                  + Yellow tint
      │      │                  + Timer to revert
      │      │
      │      └─ PowerUpWeapon → hero.upgradeWeapon()
      │                         + Increment weaponLevel (0→5)
      │                         + Update weaponStats
      │                         + Orange tint feedback
      │
      ├──> Play pickup animation (scale up + fade out)
      │
      ├──> Remove from dropper.activeItems[]
      │
      └──> Destroy item (150ms delay for animation)


┌─────────────────────────────────────────────────────────────────────────────┐
│                      7. HERO STATE CHANGES                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────┐
    │  SCORE BONUS                                           │
    │  ─────────────────────────────────────────────────     │
    │  hero.score += points                                  │
    │  Emit: 'game:score_change'                             │
    │  HUD updates automatically                             │
    └────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────┐
    │  EXTRA LIFE                                            │
    │  ─────────────────────────────────────────────────     │
    │  hero.lives = min(lives + 1, maxLives)                 │
    │  Emit: 'hero:life_gained'                              │
    │  HUD shows extra life icon                             │
    └────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────┐
    │  SHIELD (Timed Invulnerability)                        │
    │  ─────────────────────────────────────────────────     │
    │  hero.hasShield = true                                 │
    │  hero.isInvulnerable = true                            │
    │  Visual: Cyan tint + alpha pulse                       │
    │  Timer: 5000ms → auto-revert                           │
    │  Non-stacking: Multiple pickups reset timer            │
    └────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────┐
    │  SPEED BOOST (Temporary Speed Increase)                │
    │  ─────────────────────────────────────────────────     │
    │  Store: hero.originalSpeed = hero.speed                │
    │  hero.speed *= 1.5                                     │
    │  Visual: Yellow tint                                   │
    │  Timer: 8000ms → revert to originalSpeed               │
    │  Non-stacking: Multiple pickups reset timer            │
    └────────────────────────────────────────────────────────┘

    ┌────────────────────────────────────────────────────────┐
    │  WEAPON UPGRADE (Progressive Levels)                   │
    │  ─────────────────────────────────────────────────     │
    │  hero.weaponLevel++  (max 5)                           │
    │  hero.weaponStats = WEAPON_LEVELS[weaponLevel]         │
    │  Emit: 'hero:weapon_upgraded'                          │
    │  Persistent until death/reset                          │
    │                                                         │
    │  Level 0: 1 shot  @ 1.0x speed                         │
    │  Level 1: 1 shot  @ 1.3x speed                         │
    │  Level 2: 2 shots @ 1.3x speed, 15° spread             │
    │  Level 3: 3 shots @ 1.3x speed, 20° spread, +10% reach │
    │  Level 4: 4 shots @ 1.5x speed, 25° spread, +20% reach │
    │  Level 5: 5 shots @ 1.5x speed, 30° spread, +30% reach │
    └────────────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────────────────────┐
│                     8. INTEGRATION POINTS                                   │
└─────────────────────────────────────────────────────────────────────────────┘

    YOUR LEVEL SCENE:
    ┌────────────────────────────────────────────────────────┐
    │  create() {                                            │
    │      this.dropper = new Dropper(this);                 │
    │  }                                                      │
    │                                                         │
    │  update(time, delta) {                                 │
    │      this.dropper.update(this.hero);                   │
    │  }                                                      │
    └────────────────────────────────────────────────────────┘

    YOUR ENEMY CLASSES:
    ┌────────────────────────────────────────────────────────┐
    │  destroy(fromScene) {                                  │
    │      if (this.scene.dropper) {                         │
    │          this.scene.dropper.dropFrom(                  │
    │              this, this.x, this.y                      │
    │          );                                            │
    │      }                                                  │
    │      super.destroy(fromScene);                         │
    │  }                                                      │
    └────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║                            SUMMARY: 3 STEPS                                 ║
╠════════════════════════════════════════════════════════════════════════════╣
║                                                                             ║
║  1. Initialize:  this.dropper = new Dropper(this);                         ║
║                                                                             ║
║  2. Update:      this.dropper.update(this.hero);                           ║
║                                                                             ║
║  3. Drop:        this.dropper.dropFrom(enemy, x, y);                       ║
║                                                                             ║
║  → Items spawn, fall, and apply effects automatically! 🎮                  ║
║                                                                             ║
╚════════════════════════════════════════════════════════════════════════════╝


┌─────────────────────────────────────────────────────────────────────────────┐
│                         ITEM MANAGEMENT                                     │
└─────────────────────────────────────────────────────────────────────────────┘

    Max Items on Screen: 8 (configurable)
         │
         ├─ New drop attempted
         │    │
         │    ├─ Count < Max → Spawn item
         │    │
         │    └─ Count >= Max → Cancel drop
         │
         └─ Auto-cleanup:
              ├─ Item picked up → Remove from activeItems[]
              ├─ Item despawned (TTL expired) → Remove from activeItems[]
              └─ Scene transition → dropper.clearAll()


┌─────────────────────────────────────────────────────────────────────────────┐
│                      CUSTOMIZATION OPTIONS                                  │
└─────────────────────────────────────────────────────────────────────────────┘

    Change Drop Chance:
    ┌────────────────────────────────────────────────────────┐
    │  new Dropper(this, { dropChance: 0.6 })               │
    │  // 60% chance instead of default 40%                  │
    └────────────────────────────────────────────────────────┘

    Change Max Items:
    ┌────────────────────────────────────────────────────────┐
    │  new Dropper(this, { maxItems: 15 })                  │
    │  // Allow up to 15 items on screen                     │
    └────────────────────────────────────────────────────────┘

    Custom Loot Table:
    ┌────────────────────────────────────────────────────────┐
    │  const customLoot = [                                  │
    │      { type: 'POWER_UP_WEAPON', weight: 50 },         │
    │      { type: 'SCORE_BONUS', weight: 50, variant: ... }│
    │  ];                                                     │
    │  new Dropper(this, { lootTable: customLoot })         │
    └────────────────────────────────────────────────────────┘

    Force Specific Drop (Testing):
    ┌────────────────────────────────────────────────────────┐
    │  dropper.dropFrom(enemy, x, y, {                       │
    │      itemType: 'POWER_UP_LIFE',                        │
    │      guaranteed: true                                  │
    │  });                                                    │
    └────────────────────────────────────────────────────────┘


╔════════════════════════════════════════════════════════════════════════════╗
║                          READY TO USE! 🚀                                   ║
║                                                                             ║
║  All systems implemented, tested, and documented.                           ║
║  Just follow the 3-step integration and you're done!                        ║
╚════════════════════════════════════════════════════════════════════════════╝
```
